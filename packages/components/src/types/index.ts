// === 组件类型定义 ===

// 示例：组件 Props 类型
export interface ComponentProps {
    // 属性定义
}

// 示例：组件事件类型
export interface ComponentEvents {
    // 事件定义
}

// 示例：组件方法类型
export interface ComponentMethods {
    // 方法定义
}